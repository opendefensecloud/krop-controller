apiVersion: apis.kcp.io/v1alpha2
kind: APIExport
metadata:
  name: kubernetesclusters.krop.opendefense.cloud
spec:
  resources:
  - group: krop.opendefense.cloud
    name: kubernetesclusters
    schema: v1alpha1.kubernetesclusters.krop.opendefense.cloud
    storage:
      crd: {}
  # M1 has no foreign children (the only child is a core ConfigMap in the
  # consumer workspace, reached through the APIExport virtual workspace with no
  # claim). permissionClaims arrive in M2 with foreign-typed children.
  permissionClaims: []
